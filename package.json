{
  "name": "overseer-mcp",
  "version": "1.0.0",
  "description": "MCP server implementing Overseer multi-agent behavior",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "overseer-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "echo 'Tests are structured but require a test framework. See test files for coverage areas.'"
  },
  "keywords": [
    "mcp",
    "overseer",
    "multi-agent",
    "project-management"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
